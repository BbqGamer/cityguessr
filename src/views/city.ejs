<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Cityguessr</title>
    <link rel="stylesheet" href="/css/base.css">
</head>

<body>
    <%- include('components/header') %>
    <div class="container">
        <div class="city">
            <div style="text-align: center;">
                <h2 style="font-weight: bold;">
                    <a href="http://wikipedia.org<%= city.url %>">
                        <%- city.name %>
                    </a>
                </h2>
                <a href="http://wikipedia.org<%= city.country_url %>" style="vertical-align: middle;">
                    <%- city.country_name %> <img src="<%- city.country_flag_url %>" alt="<%- city.country_name %>">
                </a>
            </div>
            <div>
                <p><%- city.latitude %> <%- city.longitude %></p>
            </div>
            <div>
                <p>Added by: <%- city.added_by_username %></p>
                <p>Added at: <%- city.added_at %></p><br>
            </div>
            <div>
                <p><%= city.description %></p><br>
                <p><%= city.infobox %></p>
            </div>
            <iframe
                width="800"
                height="450"
                style="border:0"
                loading="lazy"
                allowfullscreen
                referrerpolicy="no-referrer-when-downgrade"
                src="https://www.google.com/maps/embed/v1/place?key=<%= mapsApiKey %>
                    &q=<%= city.name %>&zoom=4">
            </iframe>
        </div>
    </div>
</body>
<style>
    .city {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        margin: 0 auto;
        padding: 1rem;
        width: 100%;
        max-width: 800px;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-shadow: 0 0 5px 3px rgba(0, 0, 0, 0.1);
    }

    div {
        padding: 4px;
    }

    a {
        text-decoration: none;
        color: #000;
    }
</style>
</html>