<div class="city-list">
    <% cities.forEach(function(city) { %>
        <%- include('city-card', { city: city }) %>
            <% }); %>
</div>
<style>
    .city-list {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin: 20px;
    }
</style>